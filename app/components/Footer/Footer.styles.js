import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  footer: {
    width: '100%',
    justifyContent: 'flex-end',
    alignItems: 'flex-end',
    alignSelf: 'flex-end',
  },
});
